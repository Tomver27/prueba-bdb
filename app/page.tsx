// TODO(fase e): interfaz de consulta de una sola página (ChatBox + AnswerCard/ErrorState +
// TracePanel), sin recarga entre consultas — ver docs/FRONTEND.md.
export default function Home() {
  return (
    <main className="flex flex-1 flex-col items-center justify-center p-8">
      <p className="text-sm text-zinc-500 dark:text-zinc-400">
        Interfaz de consulta pendiente de implementación (fase e).
      </p>
    </main>
  );
}
